{
    "name": "crowdfunding-backend",
    "version": "1.0.0",
    "description": "Smart contracts for CrowdFunding",
    "scripts": {
        "test": "hardhat test",
        "compile": "hardhat compile",
        "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.0",
        "typescript": "^5.0.0",
        "ts-node": "^10.9.0"
    },
    "dependencies": {
        "dotenv": "^16.3.0"
    }
}